The workspace root holds only repository-wide metadata — .gitignore (excluding Python/Node artifacts, local DB files, and editor state) and skills-lock.json (pinning the atlas-flight-booking skill from github/atlas-doc). All domain logic is delegated to child modules, with the docs child centralizing project plans, ADRs, and integration notes that guide how other children implement the Waypoint agent. The root acts as an anchor point rather than an orchestrator.