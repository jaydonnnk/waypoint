[pytest]
addopts = -m "not live and not eval"
markers =
    live: hits the real Atlas sandbox (needs keyring auth; opt-in via -m live)
    eval: calls the real Qwen/DashScope API — costs real money; opt-in via -m eval
filterwarnings =
    ignore::DeprecationWarning
